/**
 * The generic interface determines the structure of the model
 * on which the operations are performed during event sourcing
 * By default it can be anything from db model to dump object.
 */
export interface IState {
}
